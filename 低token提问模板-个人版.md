# 低 Token 提问模板（个人版）

## 使用方式
- 每次只处理一个任务，尽量开新会话。
- 先限定文件范围，再描述问题。
- 没必要的信息不要给，避免上下文膨胀。

## 通用模板（复制即用）
修改刷新为重置日期而不是重新请求

请你直接生成让cursor对话修改代码的最省token的指令。
例如下面模板
```
【任务类型】修Bug/功能开发/重构/代码审查/文档编辑/提问
【核心目标/问题】一句话说清可落地的最终结果
【明确前提&假设】列清前置判断，不确定项直接提问
【只允许查看】E:\code\paper-format\format_docs.py，
【不要做】不要全项目扫描；不要改无关代码；不要跑全量构建，不要自动执行全量测试；不要循环修复
【已知信息】报错/现象/复现步骤/已有能力（最多5行，极简表述）
【验收标准】可量化、可验证的成功条件（拒绝“能用就行”）
【输出要求】先给核心结论，再给修改点；代码改动尽量小
【如果是提问】给出指导意见后再回复这个模板
```

【任务类型】修改bug/新增需求
【核心目标/问题】
3.给图片添加题注，关于源代码中从下文读取一行这个逻辑注释不考虑，正确：先判断其有没有题注，如果没有就新增一个`1.1 题注`//均为1.1，并修改样式。
如果多张图片连续，则仅最后一张图片添加题注

【明确前提&假设】仅正文中的图片，不删除已有题注。列清前置判断，不确定项直接提问
【只允许查看】E:\code\paper-format\format_docs.py，
【不要做】不要全项目扫描；不要改无关代码；不要跑全量构建，不要自动执行全量测试；不要循环修复；不要测试我会手动测试的。
【已知信息】报错/现象/复现步骤/已有能力（最多5行，极简表述）
【验收标准】可量化、可验证的成功条件（拒绝“能用就行”）
【输出要求】先给核心结论，再给修改点；代码改动尽量小


1.前三页完全不要动(绝对禁止)
2.然后开始识别“目录”一级标题，识别“引言”一级标题，“目录标题”和目录标题到引言之间的内容删掉
3.设置引言标题格式，小三号，黑体，居中，1.5倍行距，上插入换页符，下空一行，不加粗。
    引言正文：首行缩进2个字符，左对齐，宋体，小四号，行距：固定值20磅，不加粗。
4.下面就是文章主体正文：//仅正文标题添加手敲文本编号前缀
    一级标题：小三号，黑体，居中，1.5倍行距，上下空一行，不加粗。 
    二级标题：四号，黑体，左对齐，1.5倍行距，不加粗。
    三级标题：小四号，黑体，左对齐，首行缩进2个字符，1.5倍行距，上空一行，不加粗。
    旧标题先进行清洗，去除原有前缀和后缀，如 `1.1标题：`
    正文：首行缩进2个字符，左对齐，宋体，小四号，行距：固定值20磅，不加粗。 
    改为手打前缀文本,格式与文本标题一致。
        一级标题，编号样式为“一、二、三、”
        二级标题，编号样式为“（一）（二）（三）”
        三级标题，编号样式为“1. 2. 3.”
    图片处理
        图片改为上下型环绕，注意封面第一页不要动。
        判断图片是否有图序，如果没有则读取起下面一行内容设置为图序内容，下一行内容保留不变。
        图序，图1-1则为第一章第1张图片，以此类推，图2-5则为第二章第5张图片。
        图序，居中对齐，黑体，五号；行距：固定值20磅，不加粗。
    忽略脚注，保持原样
5.直到识别 一级标题：“结语”
    结语标题：小三号，黑体，居中，1.5倍行距，上插入换页符，下空一行，不加粗。
    结语正文：首行缩进2个字符，左对齐，宋体，小四号，行距：固定值20磅，不加粗
6.然后识别到 一级标题：参考文献
    参考文献标题：小三号，黑体，居中，1.5倍行距，上插入换页符，下空一行，不加粗。
    参考文献正文：悬挂缩进2个字符，宋体，五号，左对齐，1.5倍行距，不加粗。（区别五号）
7.然后识别到，一级标题，致谢
    致谢标题：小三号，黑体，居中，1.5倍行距，上插入换页符，下空一行，不加粗。
    致谢文本：首行缩进2个字符，左对齐，宋体，五号，行距：固定值20磅，不加粗（区别五号）

//上面完成之后
//标题均为黑色
8.页码：从`引言页`开始，起始页为1，右顶格页，脚右对齐，宋体小五号
9.所有标题或内容里的数字“123456”或外文，应使用Times New Roman字体
10.最后，包括前三页全文（包括封面摘要目录），页边距页边距：上下、左右各2.5cm


1.标题是否为标题，只依赖于其标题样式，别的都不考虑，不兜底
2.标题颜色均改为纯黑色。
3.我说的是旧文档的标题的原有前缀编号去掉，均换新的，之前数字，小数点，统统不要，该用如下的新的前缀
        一级标题，编号样式为“一、二、三、”
        二级标题，编号样式为“（一）（二）（三）”
        三级标题，编号样式为“1. 2. 3.” //不考虑层级之类的


