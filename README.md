测试运行结果我自己做，你改代码就行。太几把非token了没钱。

# paper-format
格式化毕业论文

本项目用于第一步或第二步格式化毕业论文文档，python。
Python 3.13 / 3.14
1. 安装依赖：
```bash
python -m pip install -r requirements.txt
```
2. 配置 `config.json`（默认文档目录就是当前项目目录）：

## Web 方式（临时给朋友使用）

1. 启动服务（默认监听 `9001`）：
```bash
python app.py
```
2. 浏览器打开：
`http://<你的服务器IP>:9001`
3. 上传 `.docx` 后会自动下载 `*_f.docx`。


